// Photography assets. Only the two aliases actually consumed by the
// live components are exported; everything else was removed in the
// dead-code cleanup.
import heroImage from '../assets/images/image.png';
import karthikImage from '../assets/images/image1.png';

export const ASSETS = {
  heroPortrait: heroImage,
  aboutPortrait: karthikImage,
};
